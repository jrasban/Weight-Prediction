# Assumptions and Explanations

The combined data was placed into an SQL Server where it was filtered and cleansed using queries.

Missing values were address on certain features by filling them with average of all rows for continuous variables. 

The Preprocessed data has been placed here to show a complete dataset that is ready to go for a regression model. 
